'use strict';

/**
 * notify-me-when-available router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notify-me-when-available.notify-me-when-available');
