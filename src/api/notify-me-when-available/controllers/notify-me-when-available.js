'use strict';

/**
 * notify-me-when-available controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::notify-me-when-available.notify-me-when-available');
